# Azure SQL Configuration
# Fill in these values after deploying with Bicep

# Azure SQL Server (without .database.windows.net suffix)
SQL_SERVER_NAME=your-sql-server-name

# Database name (usually matches the Bicep parameter)
SQL_DATABASE=fabricMirrorDemoDb

# Admin credentials (same as used in Bicep deployment)
SQL_USER=your-admin-login
SQL_PASSWORD=your-admin-password

# Optional: Local data directory for CSV exports
LOCAL_OUTPUT_DIR=c:\VSProjects\Fabric-IPO\data

# Streaming configuration
BATCH_SIZE=50
SLEEP_SECONDS=2
